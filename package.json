{
  "name": "word-counter",
  "version": "1.0.0",
  "description": "a library that counts the number of times a word occurs in a file",
  "main": "main.js",
  "dependencies": {
    "commander": "^9.4.0"
  },
  "scripts": {
    "test": "jest",
    "lint": "eslint ./*.js --fix"
  },
  "devDependencies": {
    "jest": "^29.1.1",
    "eslint": "^8.23.0",
    "eslint-config-airbnb-base": "^15.0.0",
    "eslint-plugin-jest": "^26.8.3"
  }
}
